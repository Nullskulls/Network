SECRET_KEY=change-me-in-production
HACKCLUB_CLIENT_ID=abc123changeme
HACKCLUB_CLIENT_SECRET=abc123changeme
HACKCLUB_REDIRECT_URI=http://localhost:5000/auth/callback
APP_URL=http://localhost:5000
# Postgres: use DB_* (preferred) or DATABASE_URL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=network
DB_USER=postgres
DB_PASSWORD=your_password
# If your host requires SSL (e.g. cloud): DB_SSLMODE=require
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Slack Bot Token (xoxb-...) with users:read scope – used to resolve display names from slack_id
# SLACK_BOT_TOKEN=xoxb-your-token

